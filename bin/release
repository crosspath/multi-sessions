#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/multi_sessions/version"

version = MultiSessions::VERSION

system("git tag 'v#{version}' && git push origin 'v#{version}' && git push")
